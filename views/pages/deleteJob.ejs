<%- include('../partials/head', {title:'Supprimer une annonce'}) %>
<%- include('../partials/header') %>

<main class="content-page">
    <div class="content-page-btns">
        <a href="/jobs" class="btn btn-primary">Annuler</a>
    </div>
    <section class="form-section">
        <h2>Suppression de l'annonce : <%= job.title %></h2>
        <form action="/jobs/<%= id %>" method="delete" class="form">
            <%- include('../partials/passwordDiv') %>
            <button type="submit" class="btn btn-primary">Supprimer définitivement l'annonce</button>

        </form>
    </section>
</main>

<%- include('../partials/footer') %>